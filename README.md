# WorldHealth Vision - V0

V0 Simple: carte du monde cliquable + fiche pays + courbes d'evolution (10-15 ans) basees sur World Bank WDI.

## Docker (front + back)

Depuis `V0/` :

```bash
docker compose -f infra/docker-compose.yml up --build
```

Arret:

```bash
docker compose -f infra/docker-compose.yml down
```
