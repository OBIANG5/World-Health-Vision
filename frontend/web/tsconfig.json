{
  // Ce fichier est responsable de:
  // - definir le projet TypeScript racine du frontend
  // - referencer les configurations TS app et node
  // - permettre le build compose via tsc -b
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}
