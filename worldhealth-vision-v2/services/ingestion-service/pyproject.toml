[project]
name = "whv-ingestion-service"
version = "0.1.0"
description = "WorldHealth Vision V2 ingestion service."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115,<1.0",
  "httpx>=0.27,<0.28",
  "openpyxl>=3.1,<4.0",
  "pycountry>=24.6,<25.0",
  "pydantic-settings>=2.6,<3.0",
  "prometheus-client>=0.21,<0.22",
  "tenacity>=9.0,<10.0",
  "typer>=0.12,<0.13",
  "uvicorn[standard]>=0.32,<0.33"
]

[project.optional-dependencies]
dev = [
  "pytest>=8.3,<9.0",
  "respx>=0.22,<0.23"
]

[tool.pytest.ini_options]
pythonpath = [
  "src"
]
testpaths = [
  "tests"
]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "src"}
include-package-data = true

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"ingestion_service.config" = ["*.json"]
